import React, { useState, useMemo, useCallback } from 'react';

interface DrawerImageProps {
}

export const DrawerImage: React.FC<DrawerImageProps> = (props) => {
    return (<div>DrawerImage</div>)
};

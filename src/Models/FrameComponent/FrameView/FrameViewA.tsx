import { DrawCanvas } from "../../../Components/Pages/Router/DrawCanvas";
import { _ss } from "../../../Helpers/HelpersFns";
import { useFrameData } from "../../../Hooks/useFrameData";

export const FrameViewA = () => {
    const [[rama, rcontrol],] = useFrameData()

    return (
        <div>

        </div>
    )

}

